{
	"components": {
		"x-gegis-library-dynamic-form": {
			"innerComponents": [],
			"uiBuilder": {
				"associatedTypes": ["global.core", "global.landing-page"],
				"label": "Dynamic Form",
				"tileIcon": "./tile-icon/dynamic-form-tile-icon.svg",
				"description": "An OOTB-style record form: configure a table, record sys_id and view; the component loads field labels/types/choices and the form layout over the Table API, renders sectioned fields, and PATCHes changes on Save (or on blur when Autosave is on). Read-only and Autosave toggles included.",
				"category": "primitives"
			},
			"properties": [
				{
					"name": "table",
					"label": "Table",
					"description": "Table name to load the record from, e.g. \"incident\".",
					"fieldType": "string",
					"defaultValue": ""
				},
				{
					"name": "sysId",
					"label": "Record sys_id",
					"description": "sys_id of the record to load and save. Bind this from the page (e.g. a URL parameter).",
					"fieldType": "string",
					"defaultValue": ""
				},
				{
					"name": "view",
					"label": "Form view",
					"description": "Form view to render (sections + field order). Pass the view's sys_id (from sys_ui_view) or its name. Leave blank for the Default view.",
					"fieldType": "string",
					"defaultValue": ""
				},
				{
					"name": "readOnly",
					"label": "Read only",
					"description": "When on, every field is rendered disabled and the Save button is hidden.",
					"fieldType": "boolean",
					"defaultValue": false
				},
				{
					"name": "autosave",
					"label": "Autosave",
					"description": "When on, each field is PATCHed back to the record as soon as it loses focus / changes (no Save click needed).",
					"fieldType": "boolean",
					"defaultValue": false
				},
				{
					"name": "columns",
					"label": "Columns",
					"description": "Number of fields per row in each section.",
					"fieldType": "number",
					"defaultValue": 2
				},
				{
					"name": "saveLabel",
					"label": "Save button text",
					"description": "Label for the manual Save button.",
					"fieldType": "string",
					"defaultValue": "Save"
				},
				{
					"name": "showSave",
					"label": "Show Save button",
					"description": "When on (and not read-only), shows the Save button.",
					"fieldType": "boolean",
					"defaultValue": true
				}
			],
			"actions": [
				{
					"name": "FIELD_CHANGED",
					"label": "Field changed",
					"description": "Fired when any field changes. Payload: { name, value }.",
					"payload": [
						{ "name": "name", "label": "Field name", "description": "The changed field's column name.", "fieldType": "string" },
						{ "name": "value", "label": "Value", "description": "The new value.", "fieldType": "string" }
					]
				},
				{
					"name": "FORM_SAVED",
					"label": "Form saved",
					"description": "Fired after a successful HTTP save (Save click or autosave). Payload: { table, sysId }.",
					"payload": [
						{ "name": "table", "label": "Table", "description": "Table that was saved.", "fieldType": "string" },
						{ "name": "sysId", "label": "Record sys_id", "description": "sys_id of the saved record.", "fieldType": "string" }
					]
				},
				{
					"name": "SAVE_ERROR",
					"label": "Save error",
					"description": "Fired when an HTTP save fails. Payload: { message }.",
					"payload": [
						{ "name": "message", "label": "Message", "description": "Error message from the Table API.", "fieldType": "string" }
					]
				}
			]
		}
	},
	"scopeName": "x_gegis_library"
}
