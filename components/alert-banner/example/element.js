/* LOCAL PREVIEW ONLY. This component has zero now-* deps, so there is nothing extra to
 * register — just the element and the auto-generated playground. */
import '../src/x-gegis-library-alert-banner';
import nowUi from '../now-ui.json';
import { mountPlayground } from './playground';

mountPlayground(nowUi);
