# Component Template

This is a template component. Copy this folder when creating a new component.

Structure:

- `src/` - source files
- `dist/` - build output
- `package.json` - package metadata and scripts

Rename the `name` field in `package.json` before publishing.
